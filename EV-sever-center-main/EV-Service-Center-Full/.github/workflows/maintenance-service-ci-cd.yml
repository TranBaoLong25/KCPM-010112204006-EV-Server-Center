name: Maintenance Service CI/CD

on:
  push:
    branches:
      - main
    paths:
      - "EV-sever-center-main/EV-Service-Center-Full/services/maintenance-service/**"
      - "EV-sever-center-main/EV-Service-Center-Full/.github/workflows/maintenance-service-ci-cd.yml"

  pull_request:
    branches:
      - main

jobs:
  test:
    runs-on: ubuntu-latest

    defaults:
      run:
        working-directory: EV-sever-center-main/EV-Service-Center-Full/services/maintenance-service

    steps:
      - name: Checkout Source
        uses: actions/checkout@v4

      - name: Setup Python
        uses: actions/setup-python@v5
        with:
          python-version: "3.12"

      - name: Install Dependencies
        run: |
          python -m pip install --upgrade pip
          pip install pytest pytest-cov
          pip install Flask Flask-SQLAlchemy Flask-Migrate Flask-JWT-Extended flask-cors redis requests python-dotenv

      - name: Run Unit Tests
        run: |
          pytest -v

      - name: Run Coverage
        run: |
          pytest --cov=. --cov-report=term

  deploy:
    runs-on: ubuntu-latest
    needs: test

    defaults:
      run:
        working-directory: EV-sever-center-main/EV-Service-Center-Full/services/maintenance-service

    steps:
      - name: Checkout Source
        uses: actions/checkout@v4

      - name: Build Docker Image
        run: |
          docker build -t maintenance-service:latest .

      - name: Verify Image
        run: |
          docker images